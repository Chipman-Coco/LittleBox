package com.chipman.data.service

interface BaseService {

    companion object {
        /**
         * 默认初始页码
         */
        const val DEFAULT_PAGE_START_NUM = 0

        const val DEFAULT_PAGE_START_NUM_1 = 1
    }
}